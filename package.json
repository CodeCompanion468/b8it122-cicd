{
  "name": "b8it122-pipeline-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal workload used to demonstrate the B8IT122 CI/CD pipeline",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "lint": "node scripts/lint.js",
    "build": "node scripts/build.js",
    "check": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
